import type { ProfileStats } from '../types/profile';

export const mockProfileStats: ProfileStats = {
  pointsThisRound: 1240,
  totalWorkouts: 18,
  currentStreak: 5,
  weeklyPoints: [80, 120, 0, 200, 140, 180, 95],
};
